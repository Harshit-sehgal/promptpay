# Next.js auto-generated file (single-quote prettier output churns against
# the double-quote form `next build` regenerates). The file header says it
# should not be edited; keep prettier off it.
apps/web/next-env.d.ts

# Generated lockfile: prettier reflowing it produces thousands of lines of
# churn on every dependency change and fights pnpm's own writer.
pnpm-lock.yaml

# Build artifacts.
**/dist/**
**/.next/**
**/out/**
**/build/**
